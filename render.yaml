services:
  - type: web
    name: ai-calendar-agent
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python Ai.py/main.py"
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
